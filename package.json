{
  "name": "georgeplukov.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "rimraf node_modules/.cache && rimraf out && next build && next export && gh-pages -d out -t true"
  },
  "dependencies": {
    "next": "9.4.1",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  }
}
