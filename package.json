{
  "name": "georgeplukov.github.io",
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "rimraf node_modules/.cache && rimraf out && next build && next export && gh-pages -d out -t true -b master"
  },
  "dependencies": {
    "next": "11.1.3",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^2.0.0"
  }
}
